<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="session">  
    <insert id="saveSession">
        INSERT INTO user_session (
            user_id, 
            session
        ) VALUES (
            #{userId},
            #{session}
        ) ON DUPLICATE KEY UPDATE session = #{session}
    </insert>

    <select id="findSession">
        SELECT
            id,
            user_id,
            session,
            reg_date,
            mod_date
        FROM
            user_session
        <where>
            user_id = #{userId}
        </where>
    </select>
</mapper>
